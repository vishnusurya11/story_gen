# LLM Provider: Choose either 'ollama' or 'gemini'
LLM_PROVIDER=ollama

# Ollama Cloud Configuration (FREE - Recommended)
# RECOMMENDED: minimax-m2:cloud - Only truly free unlimited Ollama cloud model
# Other Ollama cloud models have hourly usage limits
# Steps:
#   1. Run: ollama signin
#   2. Create API key at: https://ollama.com/settings/keys
#   3. Paste your API key below
OLLAMA_BASE_URL=https://ollama.com
OLLAMA_MODEL=minimax-m2:cloud
OLLAMA_API_KEY=your_ollama_api_key_here

# OpenAI Fallback Configuration (RECOMMENDED for reliability)
# If Ollama fails (rate limit, validation error, network issue), automatically fallback to OpenAI
# Get your API key from: https://platform.openai.com/api-keys
# gpt-4o-mini is cheap (~$0.15 per 1M input tokens) and highly reliable for structured output
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_FALLBACK_MODEL=gpt-5-mini

# Google Gemini API Configuration (Alternative option - not used with fallback strategy)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
